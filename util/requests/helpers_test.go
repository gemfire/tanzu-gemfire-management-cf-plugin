package requests_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Helpers", func() {

	Context("It currently has no tests", func() {

		It("Does nothing", func() {
			Expect(nil).To(BeNil())
		})
	})
})
